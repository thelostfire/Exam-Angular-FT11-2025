export interface ProductDto {
    id: string;
    name: string;
    description: string;
    price: number;
    imageURL: string;
    isAvailable: boolean;
    createdAt: string;
    updatedAt: string;
}